package %PACKAGE%;

import io.anuke.ucore.modules.Core;

public class %MAIN_CLASS% extends Core {
	
	@Override
	public void init(){
		add(Vars.control = new Control());
		add(Vars.ui = new UI());
	}
	
}
